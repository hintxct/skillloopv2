# Required on Vercel. Use a separate database for this fictional demo.
DATABASE_URL=
# Set to true only for the isolated hackathon/demo deployment.
DEMO_MODE=true
# Optional trusted canonical deployment origin. No trailing slash.
APP_ORIGIN=
# Local-only fallback lives in .data/skillloop.sqlite; never used on Vercel.